#include <stdio.h>
Int main() {
int l, b, area;
printf ("Length of the rectangle : ");
scanf ("%d", &l);
printf ("Breadth of the rectangle : ");
scanf ("%d", &b);
area = l * b;
printf ("\nArea of the rectangle = %d * %d = %d", l, b, area);
    return 0;
}
