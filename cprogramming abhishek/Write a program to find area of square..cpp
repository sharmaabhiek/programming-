#include <stdio.h>
intmain() {
int a, area;
printf ("Enter the side of the square (a) : ");
scanf ("%d", &a);
 area = a * a;
printf ("\nArea of the square = %d * %d = %d", a, a, area);
    return 0;
}
