#include<stdio.h>
 int main(){
 	int a,b;
 	printf("enter the number\n");
 	scanf("%d%d",&a,&b);
 	if(a>b)
 	 printf("a is the biggest number");
	  else
	 printf("b is the biggest number");
	 return 0; 
 }

