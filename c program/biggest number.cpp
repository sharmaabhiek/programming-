#include<stdio.h>
 int main(){
 	int a,b;
 	printf("enter two numbers:");
 	scanf("%d%d",&a,&b);
 	if(a>b)
 	 printf(" the biggest number=%d\n",a);
	  else
	 printf("the biggest number=%d\n",b);
	 return 0; 
 }

